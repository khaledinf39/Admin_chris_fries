<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.kh_sof_dev.admin">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activities.Production"></activity>
        <activity android:name=".Activities.login_admin" />
        <activity android:name=".Activities.Users_activity"></activity>
        <activity android:name=".Activities.Odrer_activity" />
        <activity android:name=".Activities.SignUP_phoneNumer" />
        <activity android:name=".Activities.AllOdrer_activity" />

        <activity android:name=".Activities.LogoAct">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activities.MainActivity"></activity>
    </application>

    <uses-permission android:name="android.permission.INTERNET" />

</manifest>